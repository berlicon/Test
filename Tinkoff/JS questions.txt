Тинькоф вопросы:
JS
1.  Если происходит обращение к несуществующему полю экземпляра класса, то будет ли происходить поиск значения этого поля у прототипа?
2.  Чему равно значение поля прототип (prototype) у экземпляра класса?
3.  Произойдёт ли ошибка при использовании функции до её объявления?
4.  Что является замыканием функции, область где она вызвана или область где она объявлена?
5.  Какой тип данных возвращает метод бинд (bind)?
6.  Имеет ли стрелочная функция свой контекст?
7.  Правильно ли утверждение что сэттаймаут (setTimeout) создает таску (task), а промис резолв (Promise.resolve) микротаску (microtask)?
8.  Удаляет ли оператор delete свойства прототипа объекта, если применить его на свойство самого объекта?
DOM
9.  Правильно ли утверждение что свойства события таргет (target) и карэнт таргет (currentTarget) имеют всегда разные значения?
10.  Какой метод используется для остановки всплытия события?
11.  Какой метод используется для создания слушателя события?
HTTP
12.  Правильно ли передавать тело запроса методом гет (GET)?
13.  Можно ли передать квери (query) параметры в пост (POST) запросе?
14.  Какие параметры имеет куки (cookie)?
15.  В каком заголовке передается название браузера клиента?

1. да
2. undefined. а вот поле __proto__ равно ссылке на другой инстанс, если он задан у текущего инстанса
3. нет за счет хойстинга
4. обьявлена
5. function
6. нет
7. да
8. нет
9. нет, для div на который кликнули они совпадают, а для родительского div куда событие всплыло, они разные
10. event.stopPropagation()
11. element.addEventListener()

12. нет
13. да
14. name, expires, max-age, domain, value, path, secure, httponly, samesite
15. User-Agent


